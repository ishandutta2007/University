#include <iostream>
#include <vector>
#include <queue>
#include <cstdio>
#include <cstring>


using namespace std;

int a[1020];
int n;
int rs[1020], ls[1020];
int root;


void build(int rt, int l, int r)
{
    rs[]


}


int main()
{

    int T;
    scanf("%d", &T);
    while(T--)
    {
        scanf("%d", &n);

        for(int i = 1; i <= n; i++)
            scanf("%d", &a[i]);

        root = a[1];
        build(1, 1, n);

    }

    return 0;
}